# This is an example .env file. Create a .env file in the frontend directory and set your environment variables.
# VITE_API_BASE should be the base URL for your backend API.
# For development with Vite proxy, this can be empty or just "/api" if your api.js calls are like api.get('/parcels') and you want the proxy to add /api.
# Or, if your api.js calls already include /api (e.g. api.get('/api/parcels')), set VITE_API_BASE to empty or "/".
# For production, set this to your actual backend API URL (e.g., https://api.yourdomain.com)
VITE_API_BASE=/
